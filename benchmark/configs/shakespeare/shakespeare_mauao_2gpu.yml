# Configuration file of FAR training experiment

# ========== Cluster configuration ========== 
# ip address of the parameter server (need 1 GPU process)
ps_ip: localhost

# ip address of each worker:# of available gpus process on each gpu in this node
# Note that if we collocate ps and worker on same GPU, then we need to decrease this number of available processes on that GPU by 1
# E.g., master node has 4 available processes, then 1 for the ps, and worker should be set to: worker:3
worker_ips: 
    - localhost:[5,5]

## NOTE: list of commands to use
# srun -c 22 -w mauao --gres=gpu:a40:2 --pty bash
# source /nfs-share/ls985/anaconda3/bin/activate fedscale-11; export FEDSCALE_HOME=/nfs-share/ls985/FedScale; cd $FEDSCALE_HOME
# python $FEDSCALE_HOME/docker/driver.py start $FEDSCALE_HOME/benchmark/configs/shakespeare/shakespeare_mauao_2gpu.yml
# python $FEDSCALE_HOME/docker/shutdown.py shakespeare_mauao_2gpu

## NOTE: general info
# extremely slow in OpenImage data loading mode --> 15 min/round
# it takes ???MB on 1080s
# it takes 1092MB on 2080s
# it takes 1257MB on v100s
# it takes 1383MB on a40s
# in Reddit data loading mode it takes 4059M of RAM

exp_path: $FEDSCALE_HOME/fedscale/core

# Entry function of executor and aggregator under $exp_path
executor_entry: execution/executor.py

aggregator_entry: aggregation/aggregator.py

auth:
    ssh_user: ""
    ssh_private_key: ~/.ssh/id_rsa

# cmd to run before we can indeed run FAR (in order)
setup_commands:
    # - export OMP_NUM_THREADS='1'
    - source /nfs-share/ls985/anaconda3/bin/activate fedscale-11
    - export NCCL_SOCKET_IFNAME='enp1s0f0'         # Run "ifconfig" to ensure the right NIC for nccl if you have multiple NICs

# ========== Additional job configuration ========== 
# Default parameters are specified in config_parser.py, wherein more description of the parameter can be found

job_conf: 
    - ps_port: 29731
    - job_name: shakespeare_mauao_2gpu                   # Generate logs under this folder: log_path/job_name/time_stamp
    - log_path: $FEDSCALE_HOME/benchmark # Path of log files
    - num_participants: 10                      # Number of participants per round, we use K=100 in our paper, large K will be much slower
    - data_set: shakespeare                     # Dataset: openImg, google_speech, stackoverflow
    - data_dir: /datasets/FedScale/leaf_shakespeare    # Path of the dataset
    - data_map_file: /datasets/FedScale/leaf_shakespeare/client_data_mapping/train.csv              # Allocation of data to each client, turn to iid setting if not provided
    # - device_conf_file: $FEDSCALE_HOME/benchmark/dataset/data/device_info/client_device_capacity     # Path of the client trace
    # - device_avail_file: $FEDSCALE_HOME/benchmark/dataset/data/device_info/client_behave_trace
    - model: shakespeare                            # Models: e.g., shufflenet_v2_x2_0, mobilenet_v2, resnet34, albert-base-v2
    # - gradient_policy: yogi                 # {"fed-yogi", "fed-prox", "fed-avg"}, "fed-avg" by default
    - eval_interval: 5002                     # How many rounds to run a testing on the testing set
    - rounds: 105                          # Number of rounds to run this training. We use 1000 in our paper, while it may converge w/ ~400 rounds
    - filter_less: 4                       # Remove clients w/ less than 21 samples
    - num_loaders: 2
    # - yogi_eta: 3e-3 
    # - yogi_tau: 1e-8
    - local_steps: 1 # 20
    - learning_rate: 0.8
    - batch_size: 4
    - test_bsz: 10
    - malicious_factor: 4
    - task: shakespeare


